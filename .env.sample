# Image reference pushed by Jenkins (used to render manifests in pipeline)
APP_IMAGE=YOUR_AWS_ACCOUNT_ID.dkr.ecr.YOUR_AWS_REGION.amazonaws.com/YOUR_ECR_REPO:latest

# Runtime config (mirrors k8s/gateway/app-configmap.yaml)
MONGO_URI=mongodb://<EC2_PRIVATE_IP_OR_DNS>:27017
MONGO_DB=stardb
MONGO_COLLECTION=services
LANGFLOW_URL=http://langflow:7860
FLOW_ID=REPLACE_WITH_YOUR_FLOW_ID

# Optional (if your flow expects a model key via env)
# OPENAI_API_KEY=sk-...
